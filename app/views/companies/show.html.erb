<p id="notice"><%= notice %></p>


<table class="table table-dark">
  <thead>
    <tr>
      <th scope="col">Company name</th>
      <th scope="col">Email</th>
      <th scope="col">Actions</th>
    </tr>
  </thead>
  <tbody>
   
    <tr>
      <th scope="row"><%= @company.name  %></th>
      <th scope="row"><%= @company.email  %></th>
      <td><%= link_to 'Edit', edit_company_path(@company) %> |
<%= link_to 'Back', companies_path %></td>
    </tr>
 </tbody>
</table>

<div class="row">

<% @company.forms.each do |frm| %>
    <div class="col-md-4">

      <div class="card" style="color: blue;">
      <%= image_tag("company.png", class: "card-img-top") %>
  <div class="card-body">
    <h4 class="card-title"><%= frm.form_field_1 %></h4>
  </div>
  <ul class="list-group list-group-flush">
    <li class="list-group-item"><%= link_to 'Create Form', new_form_path %></li>

  </ul>
  <div class="card-body">
  </div>
</div>

</div>
<%end%>
</div>